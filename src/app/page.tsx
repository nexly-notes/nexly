import { NoteEditor } from "@/components/editor/note-editor";

/**
 * FR-001 landing: the note editor. Persistence, the note library, and routing
 * to individual notes arrive with the VS-001 walking skeleton.
 */
export default function Home() {
  return <NoteEditor />;
}
